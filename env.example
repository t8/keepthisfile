# MongoDB
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/arweavevault?retryWrites=true&w=majority

# JWT Secret (generate a strong random string)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Stripe
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
STRIPE_PRICE_PER_MB_USD=0.05
STRIPE_MIN_PRICE_USD=1.00

# Arweave Wallet
# Your Arweave wallet JSON key
ARWEAVE_KEY_JSON={"kty":"RSA","n":"...","e":"AQAB","d":"...","p":"...","q":"...","dp":"...","dq":"...","qi":"..."}

# Note: 
# - Files ≤100KB: Uses Turbo bundling service (FREE, no AR balance or credits needed)
# - Files >100KB: Uses direct Arweave upload (requires AR balance in wallet)
# The wallet must be funded with AR tokens for larger file uploads

# Email (SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=your-email@gmail.com

# Application
NEXT_PUBLIC_BASE_URL=http://localhost:5173
MAX_FILE_BYTES=26214400
# MAX_FILE_BYTES is in bytes (default: 25MB = 26214400)

# Vercel (automatically set, but can override)
# VERCEL_URL=your-app.vercel.app

